---
name: onegai
description: 本人にしかできない手作業（外部サービスの登録・本人確認・鍵の投入・可否の判断）を、スマホで開ける一枚のページにして渡す。ガイドを読みながらツータップ以内で終わり、判断もタップで記録される。チャットに手順を書き並べる代わりに使う。「GA4 と繋いで」「アカウント作って」「本人確認して」「鍵を入れて」「どれにするか選んで」のように、作業が本人の手待ちで止まったときに使う。
---

# onegai — 本人にしかできない作業を一枚のページで渡す

外部サービスの管理画面は Claude には触れない。チャットに手順を書き並べる代わりに、
**スマホで開ける作業台**を一枚渡す。上から順にタップするだけで終わる状態にする。

典型例: GA4 と接続する、Stripe の鍵を発行する、DNS を設定する、OAuth を通す、
20本の候補から出すものを選ぶ。

## 決まり

1. **本人にしかできないことだけ載せる。** 自分でできることは載せず、自分でやる
2. **1手順 = 1画面。** 画面が変わるなら手順を割る。1つの手順に2画面ぶん詰めない
3. **拾い読みで手が動く。** 押す場所と入れる値は `**…**` で囲む。
   **太字だけ拾えば操作できる**状態にする。全文を読ませない
4. **リンクを置いて終わりにしない。** 直リンクは実在を確認して置く（推測の URL は置かない。
   URL が無い手順は `url` を省略する）。その上で、着いた画面で何を押し何を入れるかまで書く
5. **相手が管理画面に貼る文字は `copy` に入れる。** プロパティID・Webhook の URL・DNS の値など、
   こちらが知っていて相手が入力する文字列は、本文に混ぜずに `copy` へ。手で選択させない
6. **鍵はページを通さない。** API キー・パスワード・認証コードは、発行画面へのリンクだけ置き、
   ターミナルに直接貼ってもらう。ページに入力欄は無く、`copy` にも入れない
7. **鍵の手順には「PCで」と書く。** ターミナルが要る手順は、スマホの人には実行できない
8. **これで何が進むかを一行で書く。** 所要時間も添える

## 進め方

1. 依頼を分解して、本人の手が要る部分だけ残す
2. **遷移先の画面を調べる。** URL の実在だけでなく、**開いたら何が並んでいるか**まで。
   公式ヘルプを読む、ページを取得して確かめる、確かな知識があるならそれを使う。
   調べられない画面は推測で書かず、「この画面で〇〇を探してください」と正直に書く
3. `template.html` をスクラッチパッド（無ければカレントディレクトリ）に
   `onegai-<用件>.html` として複製し、**1行目の `<title>` と、その下の `DATA` を書き換える**。
   それ以外は触らない
4. 下の方法で渡し、返事を待つ

### DATA の形

```js
title:    "用件名"                        // <title> タグにも同じ文言を入れる
unblocks: "これが終わると何が進むか＋所要時間。一行"
why:      "なぜ Claude でなく本人なのか。省略可"
steps:    [ { id, label, body, url, urlLabel, copy, copyLabel } ]
choices:  { heading, items: [ { id, label, meta, why } ] }   // 省略可
```

`url` / `urlLabel` / `copy` / `copyLabel` / `why` / `choices` は省略できる。
番号（①②③）と進捗（3 / 5）はページが自動で付ける。書かなくていい。

**`</script>` を書くときは `<\/script>` と書く。** 計測タグを `copy` に入れる場合など、
そのままだとページが途中で切れる。`DATA` はページの中の script の中にあるため。

### body の書き方

`body` を読むのは、そのサービスを初めて開く人。**ただし全文は読まない。**
読まなくても手が動くように、形で書く。

| 書くもの | 記法 | 出るもの |
|---|---|---|
| 押す場所・入れる値 | `**『レコード追加』**` | 太字 |
| 行を分ける | 改行 `\n` | 段落 |
| 順にやること | 行頭 `1.` `2.` | 番号付き |
| 並ぶ注意 | 行頭 `・` | 箇条書き |

**上限を守る。** 1行は40字まで、1手順ぜんぶで200字まで。
超えたらその手順は2つ分入っている——割る。番号や箇条書きの行は短いので、行数は気にしなくていい。

```
足りない（191字・改行なし・太字なし。押す場所が3つ埋まっていて拾えない）:
  「ここが違っていると反映されません。ログインしていなければログイン画面が出ます。
   ログイン後、上部メニューの『ネームサーバー/DNS』にマウスを合わせ、開いたメニューの
   『ネームサーバーの設定』へ。対象ドメインの欄が ns1.onamae.com などお名前.com の
   ものなら大丈夫です。別の会社の名前が入っていたら設定する場所が変わるので…」

足りる（4行・119字。太字だけ拾えば手が動く）:
  "上のメニュー **『ネームサーバー/DNS』** にマウスを合わせます。\n" +
  "開いたメニューから **『ネームサーバーの設定』**。\n" +
  "対象ドメインの欄が **ns1.onamae.com** なら大丈夫です。\n" +
  "・別の会社の名前なら、そこで止めて教えてください（設定する場所が変わります）"
```

情報は落とさない。**割る。** 割り切れないなら手順を増やす。手順は多くていい——
1手順が短ければ、詰まらずに次へ進む。

割り込んでくる画面や、順番を間違えると効かない前提は、調べたときにしか見つからない。
そこが相手の止まる場所になるので、必ず一行入れる（`・` の箇条書きが向く）。

書けたら確かめる。答えられないなら足りていない。

- **太字だけ拾って操作できるか**（これが一番大事）
- リンクを押すと何が出る？（ログインや選択が挟まるなら、それも書く）
- 入れる欄はいくつある？（一つ欠けると、相手はそこで止まる）
- 何が起きたら、この手順は終わったと分かる？

なお `**` を一つも書かなかった手順は、ページが `『…』` を自動で太字にする。
保険であって、狙って書く方が良い。

### choices — 判断してもらう

「20本の候補から出すものを選ぶ」「このあと私がやることを絞る」のように、
**本人にしか決められない可否**は `choices` に載せる。手順と同じページで終わる。

- **初期値は全部オン。落とすものだけタップさせる**（選ばせるのは仕事を渡すこと）
- `why` に「なぜこれを出すのか」を一行。一瞬で落とせるように

## 渡し方

**アーティファクト**（どの端末でも開ける）: Artifact ツールで公開し、URL を単独の行で渡す。
`capabilities` は宣言しない（このページに不要で、使えないアカウントがある）。favicon は 🙏 で統一する。

**ローカルのファイル**（必ず動く）: 保存して開く。macOS は `open <file>`、Linux は `xdg-open <file>`、
Windows は `start <file>`。開いたことを伝える。Artifact ツールが無い環境ならこちら。

どちらでもチェックは端末に保存されるので、途中で閉じても、リンク先を見て戻っても消えない。

## 結果の読み方

こういう塊が貼られる。

```
[onegai] 独自ドメインを繋ぐ
できた: s1 ネームサーバーを確かめる / s2 DNS設定へ進む
つまずいた: s3 A レコードを追加する
まだ: s4 www の CNAME を追加 / s5 確定させる
外した: p2 証明書の発行を見届ける
```

- **つまずいた** = そこで止まっている。**最優先で、その手順のどこで詰まったかだけを聞く**。
  先の手順の話をしない
- **まだ** = 飛ばしたとは限らない。単に届いていないこともある。
  つまずいたが無くて まだ がある場合だけ、一言確認してから進む
- **外した** = その候補は要らないと判断された。理由は聞かない
- 全部 できた なら、確認せずそのまま作業を続ける

## やってはいけない形

- 「念のため」チャットにも手順を並べる（そちらを読まれて、ページの意味が消える）
- 一つの手順に長い一段落を書く（**これが一番多い失敗**。読む量ではなく、読まないと手が動かない量が問題）
- 太字を使わず、全文を読ませる
- リンクを置いて、着いた画面での操作を書かずに済ませる
- 画面を確かめずに操作を推測で書く（URL と同じく、推測は置かない）
- 相手が貼る文字を `body` に混ぜて、手で選択させる（`copy` に入れる）
- 鍵・パスワード・認証コードの入力欄を置く
- 判断を「どれがいいですか？」とチャットで聞く（`choices` に載せてタップで取る）
- 開いたページや URL でなく、ファイルの場所を伝えて終わる
- Claude 自身ができる作業をページに混ぜる
