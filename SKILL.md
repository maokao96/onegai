---
name: onegai
description: 本人にしかできない手作業（サービスの登録・本人確認・鍵の投入・候補の可否判断）を、直リンクとワンタップの選択が並んだ一枚のページにして渡す。チャットに手順を書き並べる代わりに使う。「登録して」「本人確認」「鍵を入れて」「どれにするか選んで」と頼むとき、作業が本人の手待ちで止まったとき、そして「手順を書いておきました」で終わりかけたときに使う。
---

# onegai — 本人への依頼を一枚のページにする

作業が人の手で止まったら、手順をチャットに書かない。依頼の全部が載った一枚を作って、リンクを渡す。

## なぜページなのか

チャットの手順は、読む人に三つの負担をかける。どこまでやったかを探して戻る負担、URL を打ち直したり画面を探す負担、
「どれをもう決めたか」を頭に置いておく負担。ページはこの三つを引き受ける——位置を保ち、各画面へ直接繋ぎ、選択を覚える。

**基準は見た目ではなく、タップ数と判断の数。** 以下の決まりは全部、そのどちらかを減らすために在る。

## 決まり

1. **本人にしかできないことだけ載せる。** 自分でできることはページに出さず、自分でやる
2. **各手順に本物のリンクを付ける。** その画面へ直接飛ぶ URL を調べる。推測の URL を置かない
3. **候補は全部オンで出す。** 落とすものだけタップさせる。「選んでください」は仕事を相手に返す形
4. **鍵をページに通さない。** API キー・パスワード・認証コードは、発行場所へのリンクだけ置き、
   ターミナルに直接貼ってもらう。ページは受け取らない
5. **戻りはコピーボタン一つ。** 保存ボタン・ログイン・アカウントを作らせない
6. **これで何が進むかを一行で書く。** 終わったら次に何が起きるか

## 手順

1. 依頼を分解する。本人の手が要る部分だけ残し、各手順の実 URL を調べる
2. `template.html` を作業場にコピーし、冒頭の `DATA` だけ書き換える。他は触らない
3. 下の方法で渡し、返事を待つ。結果が貼られたら、そのまま作業を続ける

## 渡し方

使える方を選ぶ。

**アーティファクト**（どの端末でも開ける・スマホ可）: Artifact ツールで公開し、URL を単独の行で渡す。
`capabilities` は宣言しないこと——このページには不要で、アカウントによっては使えない。

**ローカルのファイル**（必ず動く）: 保存して開く。macOS は `open <file>`、Linux は `xdg-open <file>`、
Windows は `start <file>`。開いたことを伝える。Artifact ツールが無い環境か、その機械でしか完結しない依頼のときはこちら。

## 結果の読み方

こういう塊が貼られる。

```
[handoff] Stripe を使えるようにする — 2026-08-31 15:42
done: s1, s2
skip: s3
out: E4, E7
note: E7 は去年の価格だった
```

`done` は完了、`skip` は意図して飛ばした手順、`out` は落とされた候補。
一覧の中で `out` に無いものは全部通っている。`note` は自由記述で、無いこともある。

読んだらそのまま作業を進める。「これでよろしいですか」と聞き返さない。

## やってはいけない形

- 「念のため」チャットにも手順を並べる（そちらを読まれて、位置を見失う）
- 表示するだけのページを作り、判断はチャットでさせる
- 候補を全部オフで出して「要るものを選んで」と言う
- 鍵・パスワード・認証コードの入力欄を置く
- 開いたページや URL でなく、ファイルの場所を伝えて終わる
