---
name: onegai
description: 外部サービスの設定・登録・本人確認・鍵の発行など、本人にしかできない手作業を、直リンク付きのチェックリスト一枚にして渡す。チャットに手順を書き並べる代わりに使う。「GA4 と繋いで」「アカウント作って」「本人確認して」「鍵を入れて」のように、作業が本人の手待ちで止まったときに使う。
---

# onegai — 本人にしかできない作業を一枚のページで渡す

外部サービスの管理画面での作業は、Claude には触れない。チャットに手順を書き並べる代わりに、
リンク付きのチェックリストを一枚渡す。

典型例: GA4 と接続する、Stripe の鍵を発行する、ドメインの DNS を設定する、Google の OAuth を通す。

## 決まり

1. **本人にしかできないことだけ載せる。** 自分でできることは載せず、自分でやる
2. **URL が分かる手順には直リンクを付ける。** 実際に存在する URL を確認して置く。
   推測の URL は置かない。URL が無い手順（電話・対面など）は `url` を省略する
3. **相手が管理画面に貼る文字は `copy` に入れる。** プロパティID・Webhook の URL・DNS レコードの値など、
   こちらが知っていて相手が入力する文字列は、本文に書かずに `copy` へ。
   画面に表示された上でボタン一つでコピーできる。手で選択させない
4. **鍵はページを通さない。** API キー・パスワード・認証コードは、発行画面へのリンクだけ置き、
   ターミナルに直接貼ってもらう。ページに入力欄は無く、`copy` にも入れない
5. **鍵の手順には「PCで」と書く。** ターミナルが要る手順は、スマホでページを見ている人には実行できない
6. **これで何が進むかを一行で書く。** 終わったら次に何ができるようになるか

## 進め方

1. 依頼を分解して、本人の手が要る部分だけ残す。各手順の URL を実際に確認する
2. `template.html` をスクラッチパッド（無ければカレントディレクトリ）に
   `onegai-<用件>.html` として複製し、**1行目の `<title>` タグの中身と、その下の `DATA` を書き換える**。
   それ以外は触らない
3. 下の方法で渡し、返事を待つ

### DATA の形

```js
title:    "用件名"                          // <title> タグにも同じ文言を入れる
unblocks: "これが終わると何が進むか。一行"
steps: [ { id, label, body, url, urlLabel, copy, copyLabel } ]
```

`url` / `urlLabel` / `copy` / `copyLabel` は省略できる。`copy` を書くとその文字が
カードに表示され、隣にコピーボタンが出る。`copyLabel` の既定は「コピー」。

## 渡し方

**アーティファクト**（どの端末でも開ける）: Artifact ツールで公開し、URL を単独の行で渡す。
`capabilities` は宣言しない（このページに不要で、使えないアカウントがある）。favicon は 🙏 で統一する。

**ローカルのファイル**（必ず動く）: 保存して開く。macOS は `open <file>`、Linux は `xdg-open <file>`、
Windows は `start <file>`。開いたことを伝える。Artifact ツールが無い環境ならこちら。
なおローカルで開くとコピーボタンが効かないことがあり、その場合はページが選択用の欄を出す。

## 結果の読み方

こういう塊が貼られる。

```
[onegai] GA4 と Claude を繋ぐ
done: s1, s2
yet: s3
```

`done` はチェックが付いた手順、`yet` は付かなかった手順。
**`yet` は「意図して飛ばした」とは限らない**——単に途中で止まっただけのこともある。
`yet` があるときは、そこで詰まっていないか一言聞いてから先に進む。
`yet` が無ければ、確認せずそのまま作業を続ける。

## やってはいけない形

- 「念のため」チャットにも手順を並べる（そちらを読まれて、ページの意味が消える）
- 相手が貼る文字を `body` の文章に混ぜて、手で選択させる（`copy` に入れる）
- 鍵・パスワード・認証コードの入力欄を置く
- 開いたページや URL でなく、ファイルの場所を伝えて終わる
- Claude 自身ができる作業をページに混ぜる
